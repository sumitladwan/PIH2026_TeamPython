# NPM Configuration for HackShield Portal

# Use modern npm install behavior (avoids deprecation warnings)

# Strict dependency resolution

# Use the newer omit syntax instead of deprecated --production flag
omit=dev
legacy-peer-deps=false
strict-peer-deps=false
